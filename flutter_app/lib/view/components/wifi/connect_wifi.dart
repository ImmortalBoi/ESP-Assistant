import 'package:flutter/material.dart';

class ConnectWifi extends StatelessWidget {
  // TODO Build widget so that when a button is pressed this shows up and connects to the wifi, call the connectESPWifi function in the wifi controller
  const ConnectWifi(this.name, {super.key});
  final String name;
  @override
  Widget build(BuildContext context) {
    return;
  }
}
